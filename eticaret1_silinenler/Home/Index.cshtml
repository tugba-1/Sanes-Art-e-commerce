@*@model eticaret1.Models.Productt*@
@*@model List<eticaret1.Models.Productt>*@
@*@{
        var categories = Model.Categorys;
    }*@

<!--@{
//Layout = "_Layout";
}

<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8" />
    <title>Home</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.0-beta2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-BmbxuPwQa2lc/FVzBcNJ7UAyJxM6wuqIj61tLrc4wSX0szH/Ev+nYRRuWlolflfl" crossorigin="anonymous">
</head>
<body>
    <header>
        <div class="jumbotron jumbotron-fluid">
            <div class="container">
                <h1 class="display-4">Magaza</h1>
            </div>
        </div>
    </header>
    <main>
        <div class="container">
            <div class="row">-->
                @*@foreach (var categoriess in Model.Categorys)
                    {
                        <ul class="nav flex-column">
                             <li><h2>@categoriess.CategoryDescription</h2></li>
                        </ul>
                    }*@
                @*@foreach (var productss in Model.Products)
                    {*@
                <!--<div class="col-sm-3">
                    <div class="card">-->
                        @*<img class="card-img-top" src="@Model.ImageUrl" alt="" class="rounded" />*@
                        <!--<div class="card-body">
                            <h2 class="card-title">@Model.Namee</h2>
                            <h2 class="card-title">@Model.Pricee</h2>
                            <a href="" class="btn btn-primary">İncele</a>
                        </div>
                    </div>
                </div>-->
                @*}*@
            <!--</div>
        </div>
    </main>
</body>
</html>-->