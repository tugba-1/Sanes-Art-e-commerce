 @model List<eticaret_entity.Models.Category>
 

<div class="list-group">
    @*<a asp-controller="/products" asp-action="List" class="list-group-item list-group-item-action">Tüm Kategoriler</a>*@

    @foreach (var category in Model)
    {
     <a asp-controller="Home" asp-action="List" asp-route-category="@category.Name" class="list-group-item list-group-item-action @(ViewBag.SelectedCategory == category.Id.ToString()?"active":"") ">
     @category.Name
     </a>
    }
</div>   