 @model eticaret_entity.Models.Category
 
 <div class="list-group">
    @foreach (var category in Model.Name)
    {
        <a href="#" class="list-group-item list-group-item-action">@category</a>
    }
</div>   